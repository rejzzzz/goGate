global:
    scrape_interval: 15s
    evaluation_interval: 15s
    external_labels:
        monitor: "api-gateway"

scrape_configs:
    - job_name: "gateway"
      static_configs:
          - targets: ["gateway:8080"]
      metrics_path: "/metrics"
      scrape_interval: 5s
